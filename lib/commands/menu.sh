#!/usr/bin/env bash
# lib/commands/menu.sh - Platzhalter. Das echte fzf-Menü (Preview-Pane,
# Enter=ausführen, Ctrl-E=bearbeiten) folgt in einem späteren Schritt, s. 5.4.
# Zeigt hier vor allem: wie ein Befehl die Guards aus 1.5 nutzt.

cmd_menu() {
    require_full_install
    require_fzf

    echo "[Platzhalter] Guards bestanden - hier würde das fzf-Menü starten."
}
